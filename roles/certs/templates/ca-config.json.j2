{
    "signing":{
        "default":{
            "expiry":"{{cert.expiry}}"
        },
        "profiles":{
            "kubernetes":{
                "expiry":"{{cert.expiry}}",
                "usages":[
                    "signing",
                    "key encipherment",
                    "server auth",
                    "client auth"
                ]
            }
        }
    }
}